{#
/**
 * @file
 * Default theme implementation to embed a kaltura url.
 *
 * Available variables:
 * - width: The width of the iframe.
 * - height: The height of the iframe.
 * - partnerId
 * - playerId
 * - entryId
 * - uiConfId
 *
 * @see template_preprocess_media_kaltura_embed()
 *
 * @ingroup themeable
 */
#}
<script type="text/javascript" src="https://cdnapisec.kaltura.com/p/{{ partnerId }}/sp/{{ partnerId }}00/embedIframeJs/uiconf_id/{{ uiConfId }}/partner_id/{{ partnerId }}?entry_id={{ entryId }}&amp;playerId={{ playerId }}&amp;autoembed=true&amp;width={{ width }}&amp;height={{ height }}"></script>
